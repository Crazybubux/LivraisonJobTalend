package routines;

public class TestRoutine {

    public static String CutStringOnSpace(String texte, Integer position) {
    	String[] tab = texte.split(" ");
    	if (tab.length-1 < position) {
    		return null;
    		}
    	else
    		return tab[position];
    }
}
